<div class="bg-white" data-controller="quiz">
  <div class="py-24 px-6 sm:px-6 sm:py-32 lg:px-8">
    <div class="mx-auto max-w-2xl content-center">
      <h4 data-quiz-target="step"></h4>
      <br>
      <% @questions.each do |question| %>
        <div data-quiz-target="question">
          <label class="text-base font-medium text-gray-900"><%= question.title %></label>
          <fieldset class="mt-4">
            <legend class="sr-only">Personality Test</legend>
            <div class="space-y-4">
              <% question.answers.each do |answer| %>
                <div class="flex items-center">
                  <input id="email" name="notification-method" type="radio" checked class="h-4 w-4 border-gray-300 text-indigo-600 focus:ring-indigo-500">
                  <label for="email" class="ml-3 block text-sm font-medium text-gray-700"><%= answer.title %></label>
                </div>
              <% end %>
            </div>
          </fieldset>
        </div>
      <% end %>
      <div class="flex items-center">
        <span class="isolate inline-flex rounded-md shadow-sm">
          <button type="button" data-action="quiz#previous" data-quiz-target="previous" class="relative inline-flex items-center rounded-l-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 focus:z-10 focus:border-indigo-500 focus:outline-none focus:ring-1 focus:ring-indigo-500">Previous step</button>
          <button type="button" data-action="quiz#next" data-quiz-target="next" class="relative -ml-px inline-flex items-center rounded-r-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50 focus:z-10 focus:border-indigo-500 focus:outline-none focus:ring-1 focus:ring-indigo-500">Next step</button>
        </span>
      </div>
    </div>
  </div>
</div>
